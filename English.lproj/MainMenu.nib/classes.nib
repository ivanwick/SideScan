{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {clearLogText = id; runTcpdump = id; showStatus = id; toggleAuth = id; }; 
            CLASS = PSBController; 
            LANGUAGE = ObjC; 
            OUTLETS = {textArea = NSTextView; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}